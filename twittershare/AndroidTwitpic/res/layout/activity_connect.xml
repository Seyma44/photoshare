<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:padding="10dp">
    
    <TextView
        android:id="@+id/tv_user"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/tv_title"
        android:layout_marginTop="10dp"
        android:text="Twitter Kullanıcısı:" />

<ImageView
android:id="@+id/iv_pic"
android:layout_below="@id/btn_choose"
android:layout_width="wrap_content"
android:layout_height="wrap_content"	
android:layout_marginTop="10dp"/>

<Button
    android:id="@+id/btn_send"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_alignBaseline="@+id/btn_choose"
    android:layout_alignBottom="@+id/btn_choose"
    android:layout_alignRight="@+id/tv_user"
    android:text="Twitpic&apos;e Gönder" />

<TextView
    android:id="@+id/tv_title"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_alignParentTop="true"
    android:layout_alignRight="@+id/btn_send"
    android:layout_marginTop="19dp"
    android:text="TWITPIC&apos;E GÖNDER"
    android:textSize="35sp"
    android:textStyle="bold" />

<Button
    android:id="@+id/btn_choose"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_alignLeft="@+id/tv_user"
    android:layout_below="@+id/tv_user"
    android:layout_marginTop="20dp"
    android:layout_toLeftOf="@+id/btn_send"
    android:text="Resim Seç" />
  
</RelativeLayout>